# Stream d'images en TCP/IP


## Demo

Pour compiler :
```
$ cd ~/traitement-video-c/tcp
$ make
```

Utilisation:
```
$ ./server [port (4097 par défaut)] [port camera(0 par défaut)] &
$ ./client <Adresse serveur> <Port serveur>

```
